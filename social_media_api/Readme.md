# Social Media API

## Setup

1. Install dependencies
pip install django djangorestframework 
create models and serialize

2. Start server
python manage.py runserver

## Endpoints
POST /api/register/
POST /api/login/
GET /api/profile/
